// significan name
//Bad
const customerList = [▬];
const theTable = (customers) => {
    return theTable;
}

theTable(customerList);

// Good

const customers = [];
const customerTable = (customers) => {
    return theTable;
}

customerTable(customers);
